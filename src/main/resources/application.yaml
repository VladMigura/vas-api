spring:
  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

  datasource:
    driver-class-name: org.postgresql.Driver
    username:
    password:
    url: jdbc:postgresql://localhost:5432/vas_db

server:
  port: 8080